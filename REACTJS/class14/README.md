# Context API

# 1.create context
# 2.wrap <App/> with component where your context is created
# 3.provider context from created context
# 4.consumer context[useContext(created context) wherever you want to access context]